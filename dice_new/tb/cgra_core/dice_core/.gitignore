# VCS build outputs
build/

# Verdi runtime junk (not your session — these are auto-generated noise)
novas_dump.log
novas.conf
novas.rc
sysBusyP*
sysProgressP*

# Misc simulation artifacts (if generated outside build/)
*.fsdb
*.fsdb.*
*.vpd
*.log
*.key
